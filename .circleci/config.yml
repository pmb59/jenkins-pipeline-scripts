version: 2.1
jobs:
  lint:
    docker:
      - image: nvuillam/npm-groovy-lint
    steps:
      - checkout

      - run: |
          npm-groovy-lint --fix --format --files "*.groovy"

workflows:
  version: 2
  "lint":
    jobs:
       - lint
